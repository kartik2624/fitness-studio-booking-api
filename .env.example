# Copy this file to .env and change the value before deploying anywhere real.
# The app also runs fine without a .env file - it just falls back to an
# insecure dev default, which is fine for local testing only.
JWT_SECRET_KEY=change-this-to-a-long-random-string
